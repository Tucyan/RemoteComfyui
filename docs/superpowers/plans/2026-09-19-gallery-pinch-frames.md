# 图库、缩放与帧数优化计划

1. 先补充服务端与手机端帧数边界测试、双指缩放计算测试，确认旧实现失败。
2. 将帧数下限改为 5，保留 362 上限及 17n+5 规则；输入框和加减按钮同步更新。
3. 增加通用图片双指缩放预览组件；产物预览和电脑图库原图预览共用。
4. 生成页在手机选择左侧增加图库入口；图库中分别提供预览、保存到手机、加入参考图。
5. 运行后端/移动端测试和类型检查，重新构建 release APK；辨认现有 Gateway 进程后安全重启，并验证健康接口。
