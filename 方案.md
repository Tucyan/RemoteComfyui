# Remote ComfyUI 局域网手机客户端方案

## 1. 目标与范围

在同一局域网内，通过 React Native 手机客户端使用电脑上的 ComfyUI，手机端只呈现两个固定能力：

1. **Qwen 图片编辑**：提示词 + 1–3 张可排序参考图。
2. **MiniMax H3 参考图生视频**：提示词 + 1–9 张可排序参考图 + 分辨率 + 生成帧数。

同时提供：

- 查看任务排队、运行、成功、失败状态。
- 查看图片和视频产物，保存到手机相册或分享给其他应用。
- 浏览电脑上若干预先指定的图片目录，分页预览并选作参考图。
- 手机相册上传、多图选择、顺序调整、删除和替换。
- Windows 本地浏览器管理页，用图形界面维护图片目录、手机配对和服务状态。

本项目不在手机端复刻 ComfyUI 节点编辑器，也不允许客户端提交任意工作流。

## 2. 已核对的本机事实

- ComfyUI 安装路径：`D:\AI\ComfyUI_windows_portable_nvidia\ComfyUI_windows_portable`
- 当前 ComfyUI 版本：`0.30.0`
- 当前只监听：`127.0.0.1:8188`
- GPU：RTX 4060 Laptop 8GB，启动参数包含 `--lowvram`
- 两个目标工作流位于：`ComfyUI\user\default\workflows`

工作流能力边界：

| 能力 | Qwen 图片编辑 | MiniMax H3 视频 |
|---|---|---|
| 参考图数量 | 1–3 张 | 1–9 张 |
| 提示词 | 正向提示词；负向提示词建议固定在服务端 | 支持 `<Picture 1>` 等引用标签 |
| 输出 | PNG | 带同步音频的 MP4 |
| 分辨率 | 当前由首张图及工作流逻辑决定 | 宽高必须为 32 的倍数 |
| 帧数 | 不适用 | 24fps，模型节点要求 `17n+5` 格式 |

视频原工作流以时长计算帧数：

```text
frames = roundUpTo17nPlus5(round(seconds * 24))
```

节点说明给出的主要训练区间约为 124–362 帧，也就是约 5–15 秒。手机端可以让用户调整“帧数”，同时显示换算后的时长；后端必须将输入规范化到合法帧数，并限制显存风险较高的组合。

## 3. 推荐架构

```text
React Native 手机端
        |
        | HTTP + WebSocket，令牌鉴权
        v
Remote ComfyUI Gateway（局域网唯一入口，例如 0.0.0.0:3000）
   |            |              |
   |            |              +-- 白名单图片目录、缩略图缓存
   |            +-- SQLite：任务、产物、目录索引
   +-- 127.0.0.1:8188
          ComfyUI API
```

### 3.1 ComfyUI

保持 ComfyUI 只监听 `127.0.0.1:8188`。后端在同一台电脑上调用以下接口：

- `POST /upload/image`：上传参考图到 ComfyUI input。
- `POST /prompt`：提交经过服务端生成的固定 API 工作流。
- `GET /history/{prompt_id}`：获取最终输出和错误。
- `GET /view`：读取 ComfyUI 输入、预览和输出文件。
- `GET /queue`：队列状态。
- `WS /ws`：执行进度和完成事件。

不要让 RN 直接调用 ComfyUI，也不要将 8188 改为 `0.0.0.0`。ComfyUI 原生接口允许提交任意节点图，直接暴露会绕过本项目的工作流限制和文件访问边界。

### 3.2 Gateway 后端

建议使用 **Python 3.12 + FastAPI** 单独安装运行，不使用 ComfyUI 内嵌 Python 3.13。原因是图像目录扫描、缩略图生成、WebSocket、SQLite 和 ComfyUI HTTP 调用都适合 Python，实现和部署最直接，也不会污染 ComfyUI portable 环境。

后端职责：

- 只暴露 `image-edit` 和 `video-generate` 两种任务类型。
- 校验提示词、图片数量、文件类型、文件大小、宽高和帧数。
- 将手机上传或目录选中的图片复制/上传到 ComfyUI input 的任务隔离目录。
- 从固定 API 工作流模板构造 prompt，不接受客户端上传节点图。
- 将 ComfyUI 事件转换为稳定的任务状态，并在断线后通过数据库恢复。
- 将 ComfyUI 输出映射为本项目的产物 ID，不向手机泄露电脑绝对路径。
- 对指定图片目录执行只读索引、分页、缩略图和原图流式读取。

### 3.3 React Native 客户端

建议采用 **Expo + TypeScript + Expo Router**，使用 development build，而不是只依赖 Expo Go。主要依赖建议：

- TanStack Query：请求缓存、分页、断线重试。
- Zustand：草稿、连接信息和上传队列等少量本地状态。
- `expo-image-picker`：从手机相册选择多图。
- `expo-media-library`：保存图片/视频到相册。
- `expo-sharing`：分享产物。
- `expo-video`：视频预览。
- SecureStore：保存访问令牌。

### 3.4 Windows 本地浏览器管理页

增加一个仅在 Windows 本机浏览器使用的管理前端。静态页面由 FastAPI 直接提供，启动 Gateway 后访问：

```text
http://127.0.0.1:3001/admin
```

管理页不需要桌面外壳或单独打包，也不要求安装 Node.js；生产构建后的 HTML、CSS 和 JavaScript 作为 Gateway 的静态资源部署。

管理端和手机端使用不同的监听入口：

```text
0.0.0.0:3000       手机端 API，只提供生成、任务、产物和图片库读取
127.0.0.1:3001     Windows 管理 API 和管理页面，仅本机可访问
127.0.0.1:8188     ComfyUI，仅 Gateway 可访问
```

普通浏览器不能可靠获取所选文件夹的 Windows 绝对路径，因此“选择文件夹”由 Gateway 提供受限的目录树接口实现。页面先显示可用固定磁盘，再按需展开子目录；同时允许手动粘贴绝对路径并由后端校验。目录树只返回文件夹，不枚举或下载其中的文件。

运行目录建议如下：

```text
RemoteComfyUI/
  server/
  web-admin/
  data/
    config.yaml
    remote-comfyui.db
    thumbnails/
    logs/
    backups/
```

程序不得依赖启动时的当前工作目录。所有可写数据均以项目配置的 `data` 目录为根目录。

## 4. 工作流裁剪与参数注入

### 4.1 先导出 API 工作流

现有两个 JSON 是 ComfyUI 前端工作流格式，不能直接作为 `POST /prompt` 的 `prompt` 使用。实施时需要在 ComfyUI 中分别整理后，通过开发者模式导出 **API Format**，保存为后端只读模板：

```text
server/workflows/image_qwen_2511.api.json
server/workflows/video_minimax_h3.api.json
```

模板中保留模型加载、编码、采样、解码、保存节点；删除说明节点和所有仅用于画布展示的内容。模型名、LoRA、采样器、步数、CFG、调度器、保存格式均由服务端固定，不在手机端显示。

### 4.2 Qwen 图片编辑模板

保留：

- `LoadImage` 1–3 个。
- 正向 `TextEncodeQwenImageEditPlus`。
- 固定负向提示词编码。
- 当前 Qwen 2511 INT8 模型、VAE、CLIP 和 Lightning LoRA。
- 采样、解码和 PNG 保存节点。

建议准备 3 个经过实际运行验证的模板变体：`image-1-ref`、`image-2-ref`、`image-3-ref`。这样可以避免空的第二、第三图片输入在 API 图中产生节点校验差异。后端按参考图数量选择模板，只替换：

- `LoadImage.image`
- 正向提示词
- seed（默认随机，可在后续高级选项中开放）
- 输出前缀，例如 `remote/image/{job_id}`

### 4.3 MiniMax H3 视频模板

保留当前模型、视频 VAE、音频 VAE、8-step LoRA、采样、视频/音频解码、`CreateVideo` 和 `SaveVideo`。

后端根据参考图数量动态生成 `LoadImage` 节点，并按顺序连接到：

```text
ref_images.ref_image_0 ... ref_images.ref_image_8
```

第 1 张图在提示词中对应 `<Picture 1>`。RN 的每张缩略图显示编号，并提供“插入 `<Picture N>`”按钮。拖动排序后编号随之更新。

后端只替换：

- 参考图节点与连接。
- `prompt`。
- `width`、`height`。
- `length`。
- seed。
- 输出前缀，例如 `remote/video/{job_id}`。

原工作流画布上的 `ResolutionSelector`、时长输入和数学表达式可以不进入最终 API 模板。后端直接计算并写入宽、高和合法帧数，逻辑更透明，也更易测试。

### 4.4 视频参数规则

第一版使用预设，不允许手机任意输入超大尺寸：

| 预设 | 竖屏 | 横屏 | 方形 | 适用说明 |
|---|---:|---:|---:|---|
| 省显存 | 352×608 | 608×352 | 448×448 | 8GB 显存默认 |
| 标准 | 480×864 | 864×480 | 640×640 | 建议先实测峰值显存 |
| 自定义 | 宽高为 32 的倍数 | 宽高为 32 的倍数 | 同左 | 第二阶段开放并设像素上限 |

帧数提供步进选择器，可选值为 `17n+5`。第一版建议只开放 124、175、243、294、362 帧，对应约 5.2、7.3、10.1、12.3、15.1 秒。分辨率越高、参考图越多、帧数越长，越容易在 8GB 显存上失败；应在 UI 中根据组合给出预计风险，并由服务端设置硬上限。

## 5. 客户端页面

### 5.1–5.5 React Native 手机端

#### 生成页

顶部使用“图片 / 视频”分段切换：

- 多行提示词输入框。
- 横向参考图列表：添加、删除、拖动排序、全屏预览。
- 图片来源菜单：手机相册、电脑图片库。
- 视频模式额外显示方向/清晰度预设、帧数和预计时长。
- 固定在底部的“加入队列”按钮。

提交后立即创建本地任务卡片并进入任务页，不要求手机一直保持前台。

#### 任务页

- 按“进行中 / 已完成 / 失败”过滤。
- 显示排队位置、当前阶段、进度、参数摘要、创建时间。
- 支持取消尚未开始的任务；运行中取消作为第二阶段能力。
- 失败时显示经过整理的错误和“使用原参数重试”。

#### 产物页

- 图片网格和视频封面网格，支持按类型/日期筛选。
- 图片全屏缩放，视频原比例播放。
- 保存到手机、分享、再次生成、把产物用作新任务参考图。
- 删除仅删除本项目索引还是同时删除电脑文件，必须设计为两个明确动作；第一版建议只提供“从列表移除”，不删除电脑原文件。

#### 电脑图片库

- 顶部选择已配置的目录别名，例如“素材库”“角色参考”“近期导出”。
- 缩略图网格、分页加载、按文件名搜索、按修改时间排序。
- 多选后返回生成页，并保留选择顺序。
- 手机只接收缩略图和需要预览的原图，不一次传输目录内全部原图。

#### 设置页

- 扫码或输入电脑地址完成配对。
- 显示后端、ComfyUI 和 GPU 是否在线。
- 显示当前版本、存储占用和退出配对。

### 5.6 Windows 本地管理页面

管理页面采用左侧窄导航和右侧工作区，保持桌面工具风格，不复刻 ComfyUI 节点界面。第一版包含以下页面：

#### 概览

- 显示 Gateway、ComfyUI、GPU、队列和磁盘状态。
- 显示手机应访问的局域网地址和二维码。
- 提供启动后端、停止后端、重新连接 ComfyUI 和打开数据目录操作。
- 启动失败时直接显示可读错误，并提供日志入口。

#### 图片目录

- 表格展示目录名称、路径、是否递归、图片数量、最后扫描时间和状态。
- “新增目录”通过后端目录树选择器或手动粘贴路径，然后填写手机端显示名称。
- 支持修改名称、重新选择路径、切换递归扫描、启用/停用和删除配置。
- 删除目录配置默认只移除索引和缩略图，不删除目录中的原始图片。
- 支持立即扫描、停止扫描、重建索引，并显示扫描进度和无法读取的文件数量。
- 保存前检查目录存在、可读、未与现有根目录重复或产生不必要的父子嵌套。

目录编辑采用对话框：

```text
显示名称    [角色参考                    ]
目录路径    [D:\Assets\Characters] [选择文件夹]
扫描子目录  [开]
手机端可见  [开]

                         [取消] [保存并扫描]
```

#### 手机设备

- 生成短时有效的配对码和二维码。
- 列出已配对设备、最近访问时间和令牌状态。
- 支持单独撤销设备或撤销全部设备，不显示令牌明文。

#### 工作流与存储

- 检查两个 API 工作流模板是否存在，显示所需模型和当前可用性。
- 配置 ComfyUI 地址、输出保留期限、上传大小上限和缩略图缓存上限。
- 显示任务、产物、上传缓存和缩略图的占用空间。
- 清理操作必须先展示预计删除内容并二次确认；不提供删除白名单目录原图的能力。

#### 日志与维护

- 查看最近日志，按级别过滤并复制错误详情。
- 导出诊断包，默认排除令牌、完整提示词和用户原图。
- 设置“Windows 登录后自动启动 Gateway”。
- 提供复制启动命令、打开日志目录和下载脱敏诊断包。

关闭浏览器页面不会停止 Gateway 或正在运行的生成任务。服务的启动和停止由 Windows 启动脚本或计划任务管理，不在页面内尝试终止自身进程。

## 6. 后端接口草案

| 方法 | 路径 | 用途 |
|---|---|---|
| `POST` | `/api/v1/pair` | 使用一次性配对码换取设备令牌 |
| `GET` | `/api/v1/health` | 后端、ComfyUI、GPU 与队列状态 |
| `POST` | `/api/v1/uploads/images` | 手机多图上传，返回临时资源 ID |
| `POST` | `/api/v1/jobs/image` | 创建图片任务 |
| `POST` | `/api/v1/jobs/video` | 创建视频任务 |
| `GET` | `/api/v1/jobs` | 分页读取任务 |
| `GET` | `/api/v1/jobs/{id}` | 任务详情和产物 |
| `POST` | `/api/v1/jobs/{id}/cancel` | 取消任务 |
| `GET` | `/api/v1/events` | WebSocket 任务进度推送 |
| `GET` | `/api/v1/artifacts` | 分页读取产物 |
| `GET` | `/api/v1/artifacts/{id}/content` | 预览或下载产物，支持 Range |
| `GET` | `/api/v1/libraries` | 图片目录别名列表 |
| `GET` | `/api/v1/libraries/{id}/images` | 图片分页、搜索、排序 |
| `GET` | `/api/v1/library-images/{id}/thumbnail` | 缩略图 |
| `GET` | `/api/v1/library-images/{id}/content` | 原图预览或选作参考图 |

Windows 管理 API 使用独立的 loopback 应用，不挂载到 `0.0.0.0:3000`：

| 方法 | 路径 | 用途 |
|---|---|---|
| `GET` | `/admin/api/status` | 服务、ComfyUI、GPU、网络和存储状态 |
| `GET` | `/admin/api/filesystem/drives` | 列出可用于选择的 Windows 固定磁盘 |
| `GET` | `/admin/api/filesystem/directories` | 按父目录列出直属子目录 |
| `POST` | `/admin/api/filesystem/validate` | 校验手动输入的绝对目录路径 |
| `GET/POST` | `/admin/api/libraries` | 读取或新增目录配置 |
| `PATCH/DELETE` | `/admin/api/libraries/{id}` | 修改或移除目录配置 |
| `POST` | `/admin/api/libraries/{id}/scan` | 触发增量扫描或重建索引 |
| `GET` | `/admin/api/devices` | 已配对设备列表 |
| `POST` | `/admin/api/pairing-code` | 创建一次性配对码和二维码数据 |
| `DELETE` | `/admin/api/devices/{id}` | 撤销设备 |
| `GET/PATCH` | `/admin/api/settings` | 读取或修改本机配置 |
| `GET` | `/admin/api/logs` | 分页读取脱敏日志 |

管理 API 只接受 loopback 连接，并严格校验 `Host` 和 `Origin`。首次打开页面时建立 `HttpOnly`、`SameSite=Strict` 本机会话；所有修改操作还必须携带 CSRF 令牌。管理端不启用跨域访问，也不接受手机端 Bearer token。即使局域网用户知道管理端口，也无法建立连接或借手机 API 调用管理能力。

目录树接口的附加限制：

- 只列出 `DriveType=Fixed` 的本机磁盘，不展示网络盘、可移动盘或系统保留卷；以后有明确需求再开放。
- 只返回当前目录的直属子目录，忽略文件、隐藏系统目录和无权限目录。
- 每次请求都规范化并验证绝对路径，拒绝相对路径、设备路径和路径穿越。
- 目录加载按需进行，单次返回数量设上限，避免浏览磁盘时阻塞服务。

创建视频任务示例：

```json
{
  "prompt": "<Picture 1> 中的角色走入雨夜街道，镜头缓慢后退",
  "referenceAssetIds": ["asset_01", "asset_02"],
  "resolutionPreset": "portrait_low",
  "frames": 175
}
```

任务状态统一为：

```text
queued -> uploading -> submitted -> running -> succeeded
                                      |          |
                                      +-> failed +-> canceled
```

SQLite 至少记录 `jobs`、`job_inputs`、`artifacts`、`library_roots`、`library_images`、`devices`。必须保存 ComfyUI 的 `prompt_id`，后端重启后通过 `/history/{prompt_id}` 补齐状态，不能只依赖 WebSocket 内存事件。

## 7. 指定目录图片库设计

目录只通过电脑端配置文件登记，例如：

```yaml
libraries:
  - id: character_refs
    name: 角色参考
    path: D:\Assets\Characters
    recursive: true
  - id: recent_outputs
    name: ComfyUI 产物
    path: D:\AI\ComfyUI_windows_portable_nvidia\ComfyUI_windows_portable\ComfyUI\output
    recursive: true
```

安全规则：

- API 只接收 library ID 和 image ID，绝不接收任意绝对路径。
- 每次读取都使用规范化后的真实路径检查其仍位于白名单根目录内，阻止 `..` 和符号链接越界。
- 支持 PNG、JPEG、WebP；GIF、TIFF、BMP 等格式按实际需要再开放。
- 缩略图缓存为 WebP，建议 320px 长边；列表只返回尺寸、修改时间、缩略图 URL 等元数据。
- 首次扫描可以后台进行；之后使用增量扫描或文件监视器更新。目录很大时必须分页，不能在一次请求中返回所有条目。
- 图片库默认只读。选中图片时复制到任务目录，不修改原文件。

管理端保存目录配置时采用“写临时文件 -> 校验 -> 原子替换”，并在 `data/backups` 保留最近 5 份配置。保存成功后 Gateway 热加载白名单并启动后台扫描，无需停止正在运行的生成任务；路径变更或目录移除后，相关旧索引和缩略图异步清理。

## 8. 局域网与安全

1. Gateway 监听 `0.0.0.0:3000`，ComfyUI 继续监听 `127.0.0.1:8188`。
2. Windows 防火墙只为 Gateway 端口创建“专用网络”入站规则，不开放 8188。
3. 首次配对由电脑端显示短时有效的 6 位码或二维码；配对后下发可撤销的随机令牌。
4. 所有接口除 `/pair` 和最小化的 `/health` 外均要求 Bearer token。
5. 限制上传 MIME、扩展名、单文件大小、总请求大小和图片像素数；文件名由服务端重命名。
6. 不允许客户端传入模型名、节点类型、输出路径或任意工作流 JSON。
7. 日志中隐藏令牌，不记录完整敏感提示词；提供任务记录保留天数配置。
8. 仅局域网使用的 MVP 可先用 HTTP。Android 需显式允许该局域网地址的明文流量，iOS 需声明本地网络权限和受限 ATS 例外。若以后跨网络使用，优先接入 Tailscale/WireGuard，不做公网端口映射。
9. Windows 管理页面和管理 API 只绑定 `127.0.0.1:3001`，防火墙不为该端口创建规则；管理能力不能通过手机 API 代理调用。

电脑的局域网 IP 可能变化。稳定使用可在路由器中为电脑设置 DHCP 地址保留，或在后续加入 mDNS 服务发现。

## 9. 项目结构建议

```text
RemoteComfyui/
  apps/
    mobile/                 # Expo React Native
    windows-admin/          # Windows 本地浏览器管理前端
  server/
    app/
      api/
      comfy/
      jobs/
      libraries/
      security/
    workflows/              # 固定 API 工作流模板
    admin_api/              # 仅 loopback 挂载的管理接口
    tests/
    config.example.yaml
  docs/
  scripts/
    start-server.ps1
    install-autostart.ps1
```

## 10. 分阶段实施

### 阶段 A：工作流基线

- 在 ComfyUI 中分别跑通两个原工作流并记录产物。
- 导出 API Format，制作 Qwen 1/2/3 图模板和 MiniMax 动态参考图模板。
- 编写模板测试：验证节点类型、模型名、可替换字段和输出节点。
- 用脚本直接调用 `POST /prompt`，验证所有参考图数量边界。

完成标准：1、2、3 张图的 Qwen 任务和 1、3、9 张图的 MiniMax 任务均能由 API 完成；分辨率和每个开放帧数至少各跑一次低分辨率冒烟测试。

### 阶段 B：Gateway MVP

- 完成配置、鉴权、上传、两个创建任务接口和 ComfyUI 适配器。
- 完成队列事件、历史回查、SQLite 持久化和产物流式读取。
- 完成目录白名单、缩略图和分页浏览。
- 完成仅绑定 loopback 的管理 API、目录配置热加载和配置备份。
- 添加 Windows 启动脚本和专用网络防火墙说明。

完成标准：手机浏览器或接口测试工具能从局域网提交任务、断线重连后继续查看状态，并读取产物和图片库；从其他局域网设备无法连接管理端口。

### 阶段 C：Windows 本地浏览器管理页

- 完成概览、图片目录、手机设备、工作流与存储、日志与维护页面。
- 完成固定磁盘/文件夹树、手动路径校验和开机自启动配置。
- 将管理前端生产构建作为 FastAPI 静态资源，验证直接访问和浏览器刷新路由。
- 验证路径中包含中文和空格时可以保存并扫描。
- 验证 loopback、同源、会话和 CSRF 防护。

完成标准：通过 `http://127.0.0.1:3001/admin` 无需手工编辑 YAML 即可新增、修改、停用和移除图片目录；保存后手机端能看到更新；关闭浏览器后后台任务继续执行。

### 阶段 D：RN MVP

- 完成配对、生成、任务、产物、电脑图片库五个主要流程。
- 完成多图选择/排序、视频参数、保存相册和分享。
- 处理无权限、后端离线、ComfyUI 离线、上传失败、显存不足等状态。

完成标准：Android 和 iOS 至少各用一台真机完成端到端流程；锁屏或切后台后再进入仍能恢复任务状态。

### 阶段 E：稳定性

- Windows 登录后自动启动 Gateway，并对 ComfyUI 做健康检查。
- 加入失败重试、临时上传清理、缩略图缓存配额和数据库备份。
- 根据 8GB 显存实测结果收紧视频参数矩阵。
- 增加任务取消、通知、提示词历史和产物复用等体验能力。

## 11. 测试与验收清单

- Qwen 参考图为 1、2、3 张时顺序正确；0 或 4 张被客户端和后端同时拒绝。
- MiniMax 参考图为 1、3、9 张时 `<Picture N>` 与顺序一致；10 张被拒绝。
- 视频宽高不为 32 的倍数、帧数不为 `17n+5` 时，后端返回可读错误或规范化后的明确结果。
- 两台手机同时提交时任务只进入一个受控队列，不会并发挤爆显存。
- Gateway 或 RN 重启后，运行中和已完成任务仍可恢复。
- 图片、视频支持分段读取，手机保存后可在系统相册正常打开。
- 白名单外路径、`..`、伪造 MIME、超大图片和无令牌请求均被拒绝。
- 指定目录包含上万张图片时仍能分页打开，不向手机一次性返回全部文件。
- ComfyUI 关闭时 RN 显示明确离线状态，不把任务错误显示为无限排队。
- 从 Windows 管理端新增、修改、停用和删除目录配置后，手机端列表正确更新，原始图片不被删除。
- 管理端口从另一台局域网设备连接失败，手机令牌不能调用管理 API。
- 关闭或刷新管理页面后任务继续运行，重新打开页面能恢复当前状态。

## 12. 第一版明确不做

- 手机端任意编辑、上传或运行 ComfyUI 工作流。
- 模型、LoRA、采样器、CFG、步数等专家参数切换。
- 参考视频、参考音频入口。
- 公网直接访问和多用户权限体系。
- 从手机删除电脑任意原文件。

这套边界能让第一版保持简单，同时不会妨碍后续增加新工作流。新增能力时应在服务端注册新的固定工作流类型和参数 schema，而不是把 ComfyUI 节点图暴露给客户端。
